# v1.1.5

- 主界面恢复 1.0.6 表格布局：只显示原版主列，不再把安装包配置列塞进主表格。
- 恢复左侧行选择栏，表头区域始终显示。
- 原版可见列保持：`软件名称`、`安装目录或主程序exe`、选择按钮列、`GitHub 地址`、`本地版本`、`最新版本`、`状态`、`资产筛选(可选)`。
- 所有可见列继续允许在表头分隔线处拖动调整列宽。
- 安装包支持改为后台自动识别：选到 `.exe`、`.msi`、`.msix`、`.msixbundle` 时自动静默安装；选到 `.zip`、`.7z`、`.rar` 时继续解压覆盖。
