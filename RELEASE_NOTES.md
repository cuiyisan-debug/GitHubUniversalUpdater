# v1.1.3

- 顶部按钮文案恢复为原版：`新增软件`、`删除选中`、`保存配置`、`检查更新`、`一键更新`。
- 主表格列名逐字对齐原版：`软件名称`、`安装目录或主程序exe`、`GitHub 地址`、`本地版本`、`最新版本`、`状态`、`资产筛选(可选)`。
- 每行恢复无标题 `选择` 按钮列，用于选择安装目录或主程序 `.exe`。
- 所有列显式允许拖动调整宽度。
- 安装包新增列仍保留在表格最右侧，尽量不挤占原版首屏主列。
